(defn foo [x]
  (+ x x))

(defn complicated-function [a b]
  (let [x (+ a b)]
    (apply foo [a b])))

;; (defn foo [& more]
;;   (if ))

;; this is a comment

(defn foo [x]
  (if true
    (doseq [x [1 2 3]]
      (println x))))
